- document ^ and $ behave differently
- document empty string matches: ()* etc.
- document lots of .*foo.* .*bar.* patterns cause memory explosion
- compressed transition map
- high-locality final dfa
- vertically align identifiers in .h and .cpp
- add test suite items from re2, pcre
- exception subclasses: parse, internal, execute
- .depend directory
- make dfa.cpp unit test.  move stuff from minimize.cpp
- move stuff into classes if clearer
- detail namespace
- mem hog: makeTable
- cpu hog: makeTable
- cpu hog: invert
- cpu hog: equivalenceMap
- Prune NFA states that are trivial dead ends
- Word -> 64 bit?
- template tag for bitsets NfaTag DfaTag
- use range-for where possible
- consider orderd set/map
- trim dead-end nfa states
- remove id from NfaState (if hash is ok)
